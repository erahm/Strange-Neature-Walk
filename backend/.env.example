# Database URL for Prisma + Postgres (replace password and DB host if needed)
# NOTE: Use `db` as host only when running inside docker-compose (backend container).
# If running locally (e.g., `npm run seed` on your machine), use `localhost` instead: 
#   postgresql://postgres:postgres@localhost:5432/snwdb?schema=public
DATABASE_URL="postgresql://postgres:postgres@db:5432/snwdb?schema=public"

# Server port
PORT=4000

# JWT secret for signing tokens (change for production)
JWT_SECRET=some_super_secret_here
